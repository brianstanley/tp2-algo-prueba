#include "../Tablero/Tablero.h"

class Vertice{
private:
	Tablero* datoAsociadoVertice;
public:

	/*
	 * pre: -
	 * post: se crea una instancia de la clase Vertice con un puntero a un tablero asociado
	 */
	Vertice(Tablero* dato);
};
